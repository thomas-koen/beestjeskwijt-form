jQuery(document).ready(function($) {
	const submitFormButton = $("#bk_form_submit");
	/*
	submitFormButton.click(function(event){
		let form = $(".bk_form");
		event.preventDefault();

		let form_data = {
			'action': 'bk_form_submit',
			'values': form.serializeArray()
		};
	});

	 */
});
